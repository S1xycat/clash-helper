# clash-helper v0.1.1

桌面点击隐藏问题修复版。

## 修复

- 修复点击桌面时浮窗被误判为遇到全屏窗口并自动隐藏的问题。
- 排除 Explorer 桌面和任务栏 Shell 窗口，保留真正全屏游戏/视频时自动隐藏。

## 使用

下载并运行 `ClashHelper.exe`。

如果地区识别不准确，请确认 Clash 的节点选择策略组名称是否在 README 的兼容列表中。
